import { Injectable } from '@angular/core';

@Injectable()
export class TokenService {
  constructor() { }

  getToken(): string {
    return '';
  }

  saveToken(token: string) {
    
  }

  destroyToken() {
    
  }
}
